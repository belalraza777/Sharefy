# # Frontend environment variables (Vite)
# # Copy this file to `.env.local` (or create `.env` depending on your setup)
# # and replace the placeholder values with your deployment values.

# # Base API URL used by the frontend to call backend APIs (include /api/v1 path)
# # Example: https://api.mysharefy.com/api/v1
# VITE_API_URL=https://api.example.com/api/v1

# # Socket server URL (scheme + host + optional port). If omitted,
# # the frontend will attempt to derive the socket URL from VITE_API_URL.
# # Example: https://api.example.com
# VITE_SOCKET_URL=https://api.example.com

# # The public URL where the frontend is hosted. Used by backend CORS configuration tests.
# # Example: https://app.example.com
# VITE_FRONTEND_URL=https://app.example.com

# # Optional debug flag for development
# VITE_DEBUG=true

# # Notes:
# # - Do NOT commit real secrets (API keys, tokens, passwords) into version control.
# # - For production, create a `.env.production` or set environment variables in your CI/CD or hosting provider.
# # - After editing, restart the dev server to pick up changes.
